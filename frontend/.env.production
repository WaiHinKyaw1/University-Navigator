# Production build: calls the backend hosted on Render
# Local dev still uses the Vite proxy (/api -> localhost:5000), so only
# builds targeting production embed this URL.
VITE_API_URL=https://university-navigator.onrender.com
